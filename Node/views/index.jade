extends layout

block content
  h1= title
  p Articles in database: #{count}

  a.insert(href="/article/input") Insert a new article
  br
  br
  form(name="articleById",action="article",method="get")
    label Show Article by id:
    input(type="number",name="id",value="1")
    input(type="submit",value="Show")
  if count != 0
     a(href="/article/showall") Show all
  else

  h2 By Categories
  each category, i in categories
    a(href="/article/category/#{category}") #{category}
    br
  else
      label No Cateogries yet.



