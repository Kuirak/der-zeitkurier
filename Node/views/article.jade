extends layout

block content
    h1=title
    hr
    each article in articles
        .article
            h2= article.title
            p=article.article
            each item in article.categories
             em   #{item.title},
            P Written at #{article.date}
            br
            a(href="/article/input") Insert new article
            hr


